[phases.setup]
nixPkgs = ["nodejs", "yarn"]

[phases.install]
cmds = ["yarn install"]

[phases.build]
cmds = [
  "yarn workspace @caryyon/core build",
  "yarn workspace @lattice/api build"
]

[start]
cmd = "node packages/api/dist/index.js"
